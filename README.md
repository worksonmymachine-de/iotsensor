# iotsensor
DHT temp &amp; hum sensor publishing values to a mqtt broker. Runs on a D1 mini with MicroPython image and possibly on many other MCUs with MicroPython.

Configuration and wifi credentials are externalized for easy configuration and reusability. Measure interval, thresholds for temperature and humidity and update skipping behavior can be changed in the config.py file.
